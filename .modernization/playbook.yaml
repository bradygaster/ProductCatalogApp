# AppModAgent Playbook
# Version: 1.0
#
# This file contains YOUR overrides only. Microsoft defaults are loaded automatically.
# Uncomment and modify settings below to customize the modernization strategy.
#
# To reconfigure interactively: modernize assess --reconfigure
#
# Documentation: https://aka.ms/appmodagent/playbook
#
# Generated: 2026-01-18 07:00:28

version: "1.0"

# Detected: dotnet, msmq, wcf

migration_strategy:
  framework:
    # target: net10.0  # DEFAULT
    # Options: net6 | net8 | net9 | net10

  compute:
    # preferred: container_apps  # DEFAULT
    # Options: container-apps | app-service | aks | functions
    # container-apps = Serverless containers, easiest deployment
    # app-service = Traditional PaaS, more control
    # aks = Full Kubernetes, complex workloads
    # functions = Event-driven, serverless functions

